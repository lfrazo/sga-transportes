services:
  nats-server:
    image: nats:latest
    ports:
      - "8222:8222"
  client-gateway-sga-transportes:
    build: ./client-gateway-sga-transportes
    ports:
      - "${CLIENT_GATEWAY_PORT}:3000"
    volumes:
      - ./client-gateway-sga-transportes/src:/usr/src/app/src
    command: pnpm run start:dev
    environment:
      - PORT=3000
      - NATS_SERVERS=nats://nats-server:4222
  customers-ms:
    build: ./customers-ms
    volumes:
      - ./customers-ms/src:/usr/src/app/src
    command: pnpm run start:dev
    environment:
      - PORT=3000
      - NATS_SERVERS=nats://nats-server:4222
      - DATABASE_URL=postgresql://1aad5f779c0e7798b3ac647549a9a3ba5272f020b8e42ef213176ae51191fd0b:sk_eBL_8T6dYJnph0kOq3Leg@db.prisma.io:5432/postgres?sslmode=require